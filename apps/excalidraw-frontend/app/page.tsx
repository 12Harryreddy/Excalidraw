
import {Button} from "@repo/ui/button"
export default function Home() {
  return (
    <div>
      <Button variant="primary" size="md" text="submit"></Button>
      <Button variant="secondary" size="lg" text="submit"></Button>
      <Button variant="outline" size="sm" text="submit"></Button>
      <Button variant="primary" size="lg" text="submit"></Button>
      <Button variant="secondary" size="sm" text="submit"></Button>
      <Button variant="outline" size="lg" text="submit"></Button>
    </div>
  );
}
